# Scripts

- **`dev/`** — Local daemon helpers (`local-start.sh`, `local-build.sh`, `local-stop.sh`, `local-status.sh`). Top-level `local-*.sh` in this directory forward to `dev/` for backward compatibility.
- **`ci/`** — Reserved for CI-only helpers as automation grows.
